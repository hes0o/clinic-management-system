# 🩺 Task Guide: Ela (Clinical / Doctor Module)

**Mission:** Create the efficient workspace for the Doctor.

## 📋 Phase 1: Doctor's Dashboard (Week 1)
1.  Create `Features/Doctor/DoctorDashboardView.axaml`.
2.  **The Queue List**:
    - A specific area on the left showing "Waiting Patients".
    - `ListBox` Binding to `Bassam`'s Queue data.
    - "Call Next" Button.

## ✍️ Phase 2: Diagnosis Form (Week 2)
1.  **Consultation Screen**:
    - Create `Features/Doctor/DiagnosisView.axaml`.
    - **Symptom Input**: A large `TextBox` (Accepts multi-line).
    - **Prescription**: A list where you can "Add Medicine" (Name + Dose).
2.  **History**:
    - A button "Show History" that opens a popup with previous visits.

## ⚡ Phase 4: EMR Efficiency (Week 4)
1.  **Rich Text**:
    - Ensure the Note editor supports formatting (Bold/Bullet points) if possible.
2.  **Image Attachment**:
    - Add an "Add X-Ray" button.
    - Use `OpenFileDialog` to select an image and display a thumbnail.

## ⌨️ Phase 5: Power Features (Week 5)
1.  **Keyboard Shortcuts**:
    - Bind `Ctrl+S` to "Save Diagnosis".
    - Bind `F12` to "Call Next Patient".
    - Doctors hate using the mouse!
2.  **Templates**:
    - Add a dropdown "Quick Templates" (e.g., "Flu", "Back Pain") that auto-fills the text boxes.

---
> **Tip**: Doctors hate clicking. Put everything on one screen if possible. Use "Shortcuts" (e.g., F5 to save).
