# 📺 Task Guide: Eylaf (Queue) - Phase 1 ONLY

**Mission:** Build the "Big Screen" TV Display.

## 1. Create the View
**Action**: Create file `Features/Queue/QueueDisplayView.axaml`.
**Code**:
```xml
<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="HealthCenter.Desktop.Features.Queue.QueueDisplayView">
    
    <!-- Dark Background for Contrast -->
    <Grid Background="#111111" RowDefinitions="Auto, *">
        
        <!-- Header -->
        <TextBlock Text="Now Serving / الآن نخدم" 
                   Foreground="White" HorizontalAlignment="Center" 
                   FontSize="32" Margin="0,40,0,0"/>
        
        <!-- The BIG Number -->
        <Border Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                BorderBrush="#0078D7" BorderThickness="5" CornerRadius="20" Padding="50">
            <StackPanel>
                <TextBlock Text="A-102" 
                           Foreground="#0078D7" FontWeight="Bold" 
                           FontSize="150" HorizontalAlignment="Center"/>
                
                <TextBlock Text="Room 3" 
                           Foreground="White" 
                           FontSize="60" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
```

## 2. Testing Constraints
**Action**: Run this layout and adjust the window size.
- Shrink the window -> Does the text wrap ugly? (We might need ViewBox later).
- Make the window fullscreen -> Does it look impressive?
