# 🎨 Task Guide: Merve (Design Lead)

**Mission:** Make the app look beautiful and "Simple Arabic".

## 🖌️ Phase 1: Global Styles (Week 1)
1.  **Typography**:
    - Find a good Arabic Font (Google "Tajawal" or "Cairo").
    - Add it to `Assets/Fonts`.
    - Configure `App.axaml` to use this font by default.
2.  **Colors**:
    - Define your intended color palette in `App.axaml`:
        - `PrimaryColor`: #0078D7 (Blue)
        - `BgColor`: #F3F3F3 (Light Gray)

## 🔘 Phase 2: Controls (Week 2)
1.  **Button Styles**:
    - Create a Style for `Button.Primary` (Round corners, blue background, white text).
    - Create a Style for `Button.Secondary` (Transparent, gray text).
2.  **Input Fields**:
    - Style `TextBox` to have a nice border and padding.
    - Ensure `FlowDirection="RightToLeft"` works perfectly for Arabic inputs.

## 🖼️ Phase 4: Visual Polish (Week 4)
1.  **Animations**:
    - Add subtle hover effects to all buttons (Scale up 1.05x).
    - Add "Fade In" animation when switching pages.
2.  **Custom Controls**:
    - (Optional) Design a "Human Body" vector image where doctors can click on "Head", "Chest", etc.

## 🚀 Phase 5: Marketing Assets (Week 5)
1.  **Icons**:
    - Design or find the final Application Icon (`.ico` for Windows, `.icns` for Mac).
    - Ensure it looks professional on the Desktop.
2.  **Screenshots**:
    - Take high-res screenshots of every page for the Website/Sales PDF.
3.  **Themes**:
    - Final check on "Dark Mode" if we implemented it.

---
> **Tip**: If it looks ugly, it's strictly your fault! 😉 Make it "Grandmother Friendly".
