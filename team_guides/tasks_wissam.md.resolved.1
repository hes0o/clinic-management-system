# 🏨 Task Guide: Wissam (Reception Module)

**Mission:** Create the screens for the Receptionist.

## 📝 Phase 1: Add Patient Screen (Week 1)
1.  Create `Features/Reception/AddPatientView.axaml`.
2.  **Form Fields**:
    - Name (TextBox)
    - Phone (TextBox)
    - Age (NumericUpDown)
    - "Save" (Button) -> Use Merve's `Button.Primary` style.
3.  **Binding**:
    - Bind the inputs to `AddPatientViewModel.cs`.

## 📅 Phase 2: Booking Calendar (Week 2)
1.  **Calendar View**:
    - Create `Features/Reception/BookingView.axaml`.
    - Show a large Grid representing "Time Slots" (9:00, 9:30, 10:00).
    - Allow clicking a slot to "Book Appointment".
2.  **Search**:
    - Add a Search Bar to find existing patients quickly.

## 📜 Phase 4: History View (Week 4)
1.  **Data Grid**:
    - Create `Features/Reception/PatientHistoryView.axaml`.
    - Use `DataGrid` to show a list of previous visits for a selected patient.
    - Columns: Date, Doctor Name, Diagnosis Status.

## 🧙 Phase 5: Onboarding (Week 5)
1.  **First Run Wizard**:
    - Create a "Welcome Screen" that appears only the first time the app runs.
    - Ask for the Clinic Name and Phone Number (to print on Tickets).
2.  **Help Tooltips**:
    - Add "Tooltips" to confusing buttons explaining what they do.

---
> **Tip**: Your user is a busy receptionist. Make the buttons BIG and the flow fast. "Tab" key should jump to the next input correctly.
