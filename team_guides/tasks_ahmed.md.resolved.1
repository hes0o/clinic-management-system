# 🛡️ Task Guide: Ahmed (Ops & Reliability)

**Mission:** Ensure the system never loses data and managers get their reports.

## 🪵 Phase 1: Logging & Backup (Week 1)
1.  **Logging**:
    - Install `Serilog` and `Serilog.Sinks.File`.
    - Configure it in `App.axaml.cs` to write to `Logs/log-2023.txt`.
    - Log every "Crash" or "Error".
2.  **Auto-Backup**:
    - Write a Background Service `BackupService.cs`.
    - Every 1 hour, copy `healthcenter.db` to `Backups/healthcenter_{timestamp}.db`.

## 📊 Phase 3: Reports (Week 3)
1.  **Dashboard**:
    - Install `OxyPlot.Avalonia`.
    - Create a Chart showing "Patients per Day" for the last 7 days.
2.  **Revenue Report**:
    - Create a button "Export Daily Report".
    - Generate a simple CSV file with the day's transactions.

## 📄 Phase 4: Advanced Reporting (Week 4)
1.  **PDF Generation**:
    - Install `QuestPDF`.
    - Create a beautiful PDF Invoice template with the Clinic Logo.
    - Allow printing this invoice for the patient.

## 💿 Phase 5: Installer (Week 5)
1.  **Installer Creation**:
    - Download **Inno Setup** (Free tool).
    - Create a script that bundles the published `.exe` and the `Assets` folder into a single `setup.exe`.
2.  **Licensing**:
    - Write a simple check: `if (LicenseKey != "SECRET-123") { CloseApp(); }`
    - (This is for the "Sales" part).

---
> **Tip**: You are the safety net. If the PC crashes, your backup saves the day.
