# 📺 Task Guide: Eylaf (Queue & TV Module)

**Mission:** The Public Display and Ticket Printing.

## 🖥️ Phase 1: The TV Screen (Week 1)
1.  Create `Features/Queue/QueueDisplayView.axaml`.
2.  **Design**:
    - Background: Black or Dark Blue.
    - Font: Huge (Size 120+).
    - Layout: Split screen. Left = "Now Serving", Right = "Waiting List".
3.  **Animation**:
    - Use `PageTransition` to slide numbers in when they change.

## 🖨️ Phase 3: Printing & Audio (Week 3)
1.  **Printing**:
    - Write a helper `TicketPrinter.cs`.
    - Use `System.Drawing.Printing` to send a small text ("A-101") to the default printer.
2.  **Audio**:
    - Research a TTS (Text-to-Speech) library compatible with .NET 10.
    - Make the app say "Number 101, Room 3" when `OnQueueChanged` fires.

## 🧐 Phase 4: Validation (Week 4)
1.  **Audio Polish**:
    - Ensure the TTS pronounces Arabic names correctly (or switch to English numbers if needed).
2.  **Screen Logic**:
    - What happens if the queue is empty? Show a "Welcome to our Clinic" slideshow instead of a blank screen.

## 🦾 Phase 5: Hardware Test (Week 5)
1.  **Real Hardware**:
    - Connect a real Thermal Printer (EPSON/XPrinter).
    - Test if the margins are correct.
2.  **Accessibility**:
    - Ensure the high-contrast colors on the TV screen are readable for elderly patients.

---
> **Tip**: Test your screen from 3 meters away. If you can't read it, make the font bigger!
